generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

model sinapi {
  code        Int    @id
  description String
  unit        String
  unitPrice   Float
}
